# Relaxed Summary Trees

The `*.nex` files are CCD0 summary trees generated by TreeAnnotator v2.7.7, using median heights.

The corresponding `*.csv` files are the branch rates, in units yet to be determined. These files are generated by the
script `scripts/relaxed/summary_tree.py`.
