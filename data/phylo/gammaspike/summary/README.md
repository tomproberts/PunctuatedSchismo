# GammaSpike Summary Trees

The `*.nex` files are CCD0 summary trees generated by TreeAnnotator v2.7.7, using median heights.

The corresponding `*.csv` files are the burst/spike sizes (amount of punctuated change), in percentage of basic
vocabulary changed. These files are generated by the script `scripts/gammaspike/summary_tree.py`.
